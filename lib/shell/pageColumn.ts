/**
 * Shared page column — Navbar, Insights chrome, events, dashboard, chat, and
 * marketing. Nav bars must use this inner wrapper and MUST NOT add extra
 * horizontal padding on the bar itself, or content will sit narrower than the
 * logo / account row.
 */
export const PAGE_COLUMN_MAX_CLASS = "max-w-6xl";

export const PAGE_COLUMN_PAD_CLASS = "px-4 md:px-10";

export const PAGE_COLUMN_CLASS = `mx-auto w-full ${PAGE_COLUMN_MAX_CLASS} ${PAGE_COLUMN_PAD_CLASS}`;
